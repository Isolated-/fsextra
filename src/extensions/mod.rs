/// extended `File` traits + implementations
pub mod file;

/// extends `Metadata` traits + implementations
pub mod metadata;

mod tests;

/// export for convience
pub use file::FileExtended;

/// export for convience
pub use metadata::MetadataExtended;
